# backend/Procfile
#
# Route startup through the shared entrypoint so platforms that prefer Procfile
# over Docker CMD/ENTRYPOINT still honor PROCESS_TYPE and shell-based $PORT expansion.
web: sh entrypoint.sh
worker: sh entrypoint.sh worker
beat: sh entrypoint.sh beat
eventsub: sh entrypoint.sh events
