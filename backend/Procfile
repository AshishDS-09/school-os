# backend/Procfile

web: uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2
worker: celery -A app.tasks worker --loglevel=info --pool=solo --concurrency=2
beat: celery -A app.tasks beat --loglevel=info
eventsub: python -m app.events.subscriber